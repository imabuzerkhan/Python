# lets talk about the index in string what is it and how its work so lets discuss about postive index 
# ? it always start 0 in python

name = "Mohammad"
index = name[4]

# ? can we assign the new valuue to the index no its possible 
#! name[4] = p
print("the index of 2 no is what:" , index)

# ? we can learn negative index
# ? its start from last and -1 not from 0
name = 'danish'
indexn = name[-1]
print(indexn)